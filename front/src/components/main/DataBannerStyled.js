import styled from "styled-components";
import { Row, Col } from "antd";
import { theme } from "../common/styles/palette";

export const DataTable = styled(Row)``;

export const Highlight = styled.span`
  color: ${theme.primary};
  font-weight: 700;
`;
